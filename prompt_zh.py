"""
prompt_zh.py
-----------------------------------------------------------------
中文 Prompt 模板

占位符
------
{excerpt}      —— 代码片段（带行号）
{module_fqn}   —— 待测函数所在模块的 FQN（如 complex_math.complex_math）
{file_path}    —— 源文件相对路径
{error}        —— pytest 回溯
{lines}        —— 缺失行列表
{branches}     —— 缺失分支列表
"""

# ── 初始提示 ─────────────────────────────────────────────
INITIAL = """你是一名资深 Python 测试工程师。
下面给出被测代码片段（已标行号）。
目标文件：{file_path}
应从目录 `{module_fqn}` 导入要测试的对象。

**要求**
1. 编写 **pytest** 单元测试，覆盖「未执行」的行或分支。
2. 避免随机或外部依赖，确保测试可重复、快速。
3. 只需返回 **完整的测试代码**（含 import / fixture / 断言等）。
4. 依赖导入应当为 **from 仅文件名 import 待测函数名**。
5. **仅回复代码**，禁止回复讲解。

代码片段：
```python
{excerpt}
```"""

# ── 当测试运行错误 ────────────────────────────────────────
ON_ERROR = """测试运行失败，回溯如下：
{error}
请修复测试，使所有用例通过，**不要使用 mock**。
仅回复修正后的完整测试代码，禁止回复讲解。"""

# ── 当未提升覆盖率 ────────────────────────────────────────
ON_NO_GAIN = """测试可运行，但未提升覆盖率。
缺失行：{lines}
缺失分支：{branches}
请重新生成能覆盖这些位置的测试，仅回复测试代码，禁止回复讲解。"""
